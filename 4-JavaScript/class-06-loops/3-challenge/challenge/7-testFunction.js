let x = Number;

function equação(x) {
    (2 * x + (10/2))

    return  x;
}

console.log(equação);

const UserEquaçao = prompt("Digite um número para a equação");
const converterEquação = equação(x);